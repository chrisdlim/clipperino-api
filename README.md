Sample project for uploading clips!

# Requirements
- Localstack for AWS local development

# Getting started
- run `npm i` to install dependencies
- run `npm run start` to build and start the application